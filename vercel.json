{
  "buildCommand": "ROOT_DIR=app next build && cd backend && npm install && npx copyfiles server.js ../dist && cd ../app && npx copyfiles -u 1 .next/* ../dist",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "dist/app"
    }
  ]
}