{
  "buildCommand": "next build app && cd backend && npm install && copyfiles server.js ../dist && cd ../app && copyfiles -u 1 .next/* ../dist",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "dist/app"
    }
  ]
}