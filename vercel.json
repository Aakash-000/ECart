{
  "buildCommand": "npx next build ./app && cd backend && npm install && npx copyfiles server.js ../dist && npx copyfiles -u 1 app/.next/* dist/",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "dist/app"
    }
  ]
}